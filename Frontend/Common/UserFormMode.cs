namespace Frontend.Common
{
    public enum UserFormMode
    {
        NEW,
        EDIT,
        CLONE
    }

    public enum LoginType
    {
        Local,
        Centralized,
        OIDC
    }
}
